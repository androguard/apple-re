[package]
name = "ipa_vfs"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "no_std in-memory IPA / ZIP virtual filesystem"

[dependencies]
miniz_oxide = { workspace = true }

[features]
default = []
